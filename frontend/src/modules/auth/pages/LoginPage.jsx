import { PlaceholderPage } from "../../../shared/components/PlaceholderPage";

export function LoginPage() {
    return (
        <PlaceholderPage
            title="Acceso"
            description="Pantalla preparada para conectar el formulario con authApi.login."
        />
    );
}
